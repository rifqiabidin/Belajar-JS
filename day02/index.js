// const tokoBuka = true;
// const uang = 50000;

// uang > 50000 ? console.log("sugih") : console.log("kere");

const tombol = 0;

switch(tombol){
    case 1: {
        console.log(`tombol ${tombol} ditekan`);
        break;
    }
    case 2: {
        console.log(`tombol ${tombol} ditekan`);
        break;
    }
    default:{
        console.log(`tidak ada tombol ditekan`)
    }
}